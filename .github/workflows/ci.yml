# name: CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main
#       - master
#   pull_request:
#     branches:
#       - main
#       - master

# jobs:
#   build-and-test:
#     name: Build & Test
#     runs-on: ubuntu-latest

#     # We spin up a lightweight Redis instance specifically so E2E tests and Bull queues don't crash
#     services:
#       redis:
#         image: redis:alpine
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'yarn'

#       - name: Install Dependencies
#         run: yarn install --frozen-lockfile

#       - name: Lint Code
#         run: yarn lint

#       - name: Run Unit Tests
#         run: yarn test
#         env:
#           NODE_ENV: test
#           JWT_SECRET: testing_secret_key

#       - name: Run E2E Tests
#         run: yarn test:e2e
#         env:
#           NODE_ENV: test
#           USE_REDIS: true
#           REDIS_HOST: localhost
#           REDIS_PORT: 6379
#           JWT_SECRET: testing_secret_key

#       - name: Build Application
#         run: yarn build

#   deploy:
#     name: Deploy to Production
#     needs: build-and-test
#     runs-on: ubuntu-latest
#     # Only run deploy if pushing directly to main/master (not on PRs)
#     if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Deploy Placeholder
#         run: |
#           echo "🚀 Tests passed! Ready for deployment."
#           echo "Insert your deployment scripts here."
#           echo "Examples: "
#           echo " - AWS ECR / ECS: docker build & push"
#           echo " - Heroku/Render: git push heroku main"
#           echo " - Vercel/Netlify: vercel --prod"
